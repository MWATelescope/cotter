#!/bin/bash
#PBS -N JOB_NAME
#PBS -W group_list=astronomy556
#PBS -q workq
#PBS -l walltime=01:00:00
#PBS -l select=1:ncpus=12:mem=69gb
#PBS -j oe

cd ~/pbs_work

if [[ "${PBS_ARRAY_INDEX}" == "" ]] ; then
  echo Array index not set\! \(PBS_ARRAY_INDEX\)
  exit
fi
JOBID=${PBS_ARRAY_INDEX}
